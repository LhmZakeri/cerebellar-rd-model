[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "cerebellar-rd-model"
version = "0.1.0"
dependencies = [
    "numpy",
    "numba",
    "matplotlib",
    "scipy",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]